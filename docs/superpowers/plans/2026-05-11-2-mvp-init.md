# Phase 0 (MVP プロジェクト初期化) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mirror C.C. の MVP 開発基盤 (Vite + TS + Three.js + ESLint Flat config + Prettier + Vitest) を構築し、`npm run dev / build / test / lint` がすべて通る状態にする。

**Architecture:** ブラウザ SPA。Vite を開発サーバ・バンドラとして使い、Three.js で 3D 描画する。ESLint v9 系の Flat config (`eslint.config.js`) と Prettier を採用、テストランナーは Vitest (デフォルト node、必要に応じてファイル単位で jsdom 切替)。

**Tech Stack:** Node.js 20+, npm, TypeScript 5 (strict), Vite 5, Three.js 0.160+, Vitest 1.x, ESLint 9 + typescript-eslint, Prettier 3

**対象 Issue:** https://github.com/mishi5/mirror_cc/issues/2 (親: https://github.com/mishi5/mirror_cc/issues/1)
**Spec:** `docs/superpowers/specs/2026-05-11-2-mvp-init-design.md`

---

## File Structure

新規作成 / 修正するファイル:

- Create: `package.json`
- Create: `package-lock.json` (npm install で自動生成)
- Create: `tsconfig.json`
- Create: `vite.config.ts`
- Create: `eslint.config.js`
- Create: `.prettierrc.json`
- Create: `.prettierignore`
- Create: `index.html`
- Create: `src/main.ts`
- Create: `tests/smoke.test.ts`
- Create: `.gitkeep` × ディレクトリ各位置 (`public/`, `src/game/`, `src/pose/detectors/`, `src/rendering/fighters/`, `src/rendering/effects/`, `src/rendering/ui/`, `src/scenes/`, `src/audio/`, `src/debug/`, `docs/plans/`, `docs/superpowers/plans/`)
- Modify: `README.md` (セットアップ手順追記)
- 既存 `.gitignore` は十分なので変更不要

---

### Task 1: package.json と依存インストール

**Files:**
- Create: `package.json`
- Auto-generated: `package-lock.json`, `node_modules/`

- [ ] **Step 1: package.json を作成**

```json
{
  "name": "mirror_cc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "カメラによるボーン認識を利用したC.C.レモンゲーム風対戦ゲーム",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^20.0.0",
    "@types/three": "^0.160.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "globals": "^15.0.0",
    "jsdom": "^24.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
```

- [ ] **Step 2: 依存をインストール**

Run: `npm install`
Expected: `added NNN packages, and audited NNN packages in Xs` のような成功メッセージ。`package-lock.json` と `node_modules/` が生成される。エラーが出た場合は npm レジストリ接続 / 各パッケージの最新版有無を確認。

- [ ] **Step 3: インストール結果を確認**

Run: `npm ls --depth=0`
Expected: `three@0.160.x` や `vite@5.x.x` などが警告なく一覧表示される。

- [ ] **Step 4: コミット**

```bash
git add package.json package-lock.json
git commit -m "$(cat <<'EOF'
#2 chore: package.json と依存パッケージを追加

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: TypeScript 設定 (tsconfig.json)

**Files:**
- Create: `tsconfig.json`

- [ ] **Step 1: tsconfig.json を作成**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true,
    "types": ["vitest/globals", "node"]
  },
  "include": ["src", "tests", "vite.config.ts", "eslint.config.js"]
}
```

- [ ] **Step 2: tsc が空 include でも通ることを確認**

Run: `npx tsc --noEmit`
Expected: src/tests がまだ空に近いためエラーなしで完了 (exit 0)。include に該当ファイルがなくても tsc は OK。

- [ ] **Step 3: コミット**

```bash
git add tsconfig.json
git commit -m "$(cat <<'EOF'
#2 chore: TypeScript strict 設定を追加

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Vite + Vitest 設定 (vite.config.ts)

**Files:**
- Create: `vite.config.ts`

- [ ] **Step 1: vite.config.ts を作成**

```ts
/// <reference types="vitest" />
import { defineConfig } from "vite";

export default defineConfig({
  test: {
    environment: "node",
    include: ["tests/**/*.test.ts", "src/**/*.test.ts"],
  },
});
```

- [ ] **Step 2: tsc で構文・型エラーが無いことを確認**

Run: `npx tsc --noEmit`
Expected: exit 0。`vitest/globals` の reference 解決と `defineConfig` の型解決が通る。

- [ ] **Step 3: コミット**

```bash
git add vite.config.ts
git commit -m "$(cat <<'EOF'
#2 chore: Vite + Vitest 設定を追加

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: ESLint Flat config + Prettier 設定

**Files:**
- Create: `eslint.config.js`
- Create: `.prettierrc.json`
- Create: `.prettierignore`

- [ ] **Step 1: eslint.config.js を作成**

```js
import js from "@eslint/js";
import tseslint from "typescript-eslint";
import prettier from "eslint-config-prettier";
import globals from "globals";

export default [
  { ignores: ["dist/**", "node_modules/**", ".worktrees/**"] },
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    languageOptions: {
      globals: { ...globals.browser, ...globals.node },
    },
    rules: {
      "no-console": ["warn", { allow: ["warn", "error"] }],
    },
  },
  prettier,
];
```

- [ ] **Step 2: .prettierrc.json を作成**

```json
{
  "semi": true,
  "singleQuote": false,
  "trailingComma": "all",
  "printWidth": 100
}
```

- [ ] **Step 3: .prettierignore を作成**

```
node_modules
dist
coverage
.worktrees
package-lock.json
```

- [ ] **Step 4: lint コマンドが通ることを確認**

Run: `npm run lint`
Expected: まだソースがほぼ無いため exit 0 (No files matching pattern も想定。出た場合は次タスクで src/ にコードが入ったら自動的に解消する)。

- [ ] **Step 5: コミット**

```bash
git add eslint.config.js .prettierrc.json .prettierignore
git commit -m "$(cat <<'EOF'
#2 chore: ESLint Flat config と Prettier を導入

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: テスト基盤の検証 (tests/smoke.test.ts)

TDD の精神に沿い、まず Vitest 基盤が動くことを検証するテストを書く。

**Files:**
- Create: `tests/smoke.test.ts`

- [ ] **Step 1: tests/smoke.test.ts を作成**

```ts
import { describe, it, expect } from "vitest";

describe("smoke", () => {
  it("Vitest が起動し、最低限の expect が動作する", () => {
    expect(1 + 1).toBe(2);
  });

  it("TypeScript strict 型チェックでオブジェクトリテラルが推論できる", () => {
    const obj: { a: number; b: string } = { a: 1, b: "x" };
    expect(obj.a).toBe(1);
    expect(obj.b).toBe("x");
  });
});
```

- [ ] **Step 2: テストを実行し、PASS することを確認**

Run: `npm run test`
Expected:
```
 ✓ tests/smoke.test.ts (2)
   ✓ smoke
     ✓ Vitest が起動し、最低限の expect が動作する
     ✓ TypeScript strict 型チェックでオブジェクトリテラルが推論できる

 Test Files  1 passed (1)
      Tests  2 passed (2)
```
exit 0。

- [ ] **Step 3: tsc でも型エラーが出ないことを確認**

Run: `npx tsc --noEmit`
Expected: exit 0。

- [ ] **Step 4: lint も通ることを確認**

Run: `npm run lint`
Expected: exit 0 (warning 0)。

- [ ] **Step 5: コミット**

```bash
git add tests/smoke.test.ts
git commit -m "$(cat <<'EOF'
#2 test: Vitest スモークテストを追加

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: index.html と src/main.ts (Three.js 回転キューブ)

**Files:**
- Create: `index.html`
- Create: `src/main.ts`

- [ ] **Step 1: index.html を作成**

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mirror C.C.</title>
    <style>
      html,
      body {
        margin: 0;
        padding: 0;
        overflow: hidden;
        background: #000;
        color: #fff;
        font-family: system-ui, sans-serif;
      }
      #app {
        width: 100vw;
        height: 100vh;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] **Step 2: src/main.ts を作成**

CLAUDE.md の注意点に従う:
- `renderer.setSize(w, h, false)` の第3引数を `false` で明示
- `renderer.setPixelRatio(window.devicePixelRatio)` で Retina 対応

```ts
import * as THREE from "three";

const container = document.getElementById("app");
if (!container) {
  throw new Error("#app element not found");
}

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x111111);

const camera = new THREE.PerspectiveCamera(
  60,
  window.innerWidth / window.innerHeight,
  0.1,
  100,
);
camera.position.set(0, 0, 3);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(window.innerWidth, window.innerHeight, false);
container.appendChild(renderer.domElement);

const geometry = new THREE.BoxGeometry(1, 1, 1);
const material = new THREE.MeshStandardMaterial({ color: 0x44aaff });
const cube = new THREE.Mesh(geometry, material);
scene.add(cube);

const ambient = new THREE.AmbientLight(0xffffff, 0.3);
const directional = new THREE.DirectionalLight(0xffffff, 0.8);
directional.position.set(2, 2, 3);
scene.add(ambient, directional);

window.addEventListener("resize", () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight, false);
});

function animate(): void {
  requestAnimationFrame(animate);
  cube.rotation.x += 0.01;
  cube.rotation.y += 0.012;
  renderer.render(scene, camera);
}
animate();
```

- [ ] **Step 3: 型チェックを実行**

Run: `npx tsc --noEmit`
Expected: exit 0。`@types/three` 解決で THREE 名前空間が型付くこと。

- [ ] **Step 4: lint を実行**

Run: `npm run lint`
Expected: exit 0 (warning 0)。

- [ ] **Step 5: ビルドを実行**

Run: `npm run build`
Expected: `dist/` が作成され、`tsc --noEmit && vite build` の両ステップが exit 0。最後に `✓ built in Xms` のような成功メッセージ。

- [ ] **Step 6: コミット**

```bash
git add index.html src/main.ts
git commit -m "$(cat <<'EOF'
#2 feat: Three.js の最小シーン (回転キューブ) を追加

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: ディレクトリスケルトン (.gitkeep)

CLAUDE.md のディレクトリ構成に従い、空ディレクトリを `.gitkeep` で追跡する。

**Files:**
- Create: `public/.gitkeep`
- Create: `src/game/.gitkeep`
- Create: `src/pose/detectors/.gitkeep`
- Create: `src/rendering/fighters/.gitkeep`
- Create: `src/rendering/effects/.gitkeep`
- Create: `src/rendering/ui/.gitkeep`
- Create: `src/scenes/.gitkeep`
- Create: `src/audio/.gitkeep`
- Create: `src/debug/.gitkeep`
- Create: `docs/plans/.gitkeep`
- Create: `docs/superpowers/plans/.gitkeep`

(`docs/superpowers/specs/` は本 Phase の spec ファイルが存在するため `.gitkeep` 不要。`docs/superpowers/plans/` は本 plan を置いた直後に存在するため `.gitkeep` 不要だが、将来削除した時に消えないようにあえて置く)

- [ ] **Step 1: 各ディレクトリと .gitkeep を作成**

Run:
```bash
mkdir -p public src/game src/pose/detectors src/rendering/fighters src/rendering/effects src/rendering/ui src/scenes src/audio src/debug docs/plans docs/superpowers/plans
touch public/.gitkeep src/game/.gitkeep src/pose/detectors/.gitkeep src/rendering/fighters/.gitkeep src/rendering/effects/.gitkeep src/rendering/ui/.gitkeep src/scenes/.gitkeep src/audio/.gitkeep src/debug/.gitkeep docs/plans/.gitkeep docs/superpowers/plans/.gitkeep
```

Expected: 各 `.gitkeep` が作成される。

- [ ] **Step 2: 追跡確認**

Run: `git status`
Expected: 11 個の `.gitkeep` が `Untracked files` に表示される。

- [ ] **Step 3: コミット**

```bash
git add public/.gitkeep src/game/.gitkeep src/pose/detectors/.gitkeep src/rendering/fighters/.gitkeep src/rendering/effects/.gitkeep src/rendering/ui/.gitkeep src/scenes/.gitkeep src/audio/.gitkeep src/debug/.gitkeep docs/plans/.gitkeep docs/superpowers/plans/.gitkeep
git commit -m "$(cat <<'EOF'
#2 chore: ディレクトリスケルトン (.gitkeep) を追加

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: README にセットアップ手順を追記

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Write ツールで `README.md` を以下の内容で上書きする**

外側の 4 バッククォートフェンスはこの plan の表記用で、ファイル本体は内側 (3バッククォート含む) だけを書き込む:

````markdown
# Mirror C.C.

カメラによるボーン認識を利用した C.C.レモンゲーム風の対戦ゲーム。

## 技術スタック

- TypeScript (strict)
- Vite (開発サーバ・ビルド)
- Three.js (3D 描画)
- MediaPipe Tasks - Pose Landmarker (姿勢推定, Phase 1 以降)
- Vitest (テスト)
- ESLint (Flat config) + Prettier

## セットアップ

Node.js 20+ と npm が必要。

```sh
npm install
```

## 開発コマンド

| コマンド | 内容 |
| --- | --- |
| `npm run dev` | Vite 開発サーバ起動 (http://localhost:5173) |
| `npm run build` | 型チェック + 本番ビルド (`dist/`) |
| `npm run preview` | ビルド結果のプレビュー |
| `npm run test` | Vitest 全件実行 |
| `npm run test:watch` | Vitest watch モード |
| `npm run lint` | ESLint 実行 |
| `npm run format` | Prettier で整形 |
| `npx tsc --noEmit` | 型チェックのみ |

## プロジェクト構成

`CLAUDE.md` のアーキテクチャ節を参照。MVP の進捗は Issue #1 でトラッキングしている。

## ライセンス

未定。
````

- [ ] **Step 2: README が崩れていないか確認**

Run: `cat README.md`
Expected: 上記内容のまま出力される。バッククォートのエスケープが残っていないこと。

- [ ] **Step 3: コミット**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
#2 docs: README にセットアップ手順と開発コマンドを追記

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: 全件検証

**Files:** なし

- [ ] **Step 1: lint**

Run: `npm run lint`
Expected: exit 0、warning 0。

- [ ] **Step 2: 型チェック**

Run: `npx tsc --noEmit`
Expected: exit 0。

- [ ] **Step 3: テスト**

Run: `npm run test`
Expected: `Test Files 1 passed (1) / Tests 2 passed (2)`。

- [ ] **Step 4: ビルド**

Run: `npm run build`
Expected: `tsc --noEmit && vite build` 両方 exit 0。`dist/index.html` `dist/assets/*.js` が生成される。

- [ ] **Step 5: ブランチの状態確認**

Run: `git status`
Expected: `nothing to commit, working tree clean`。

- [ ] **Step 6: ログ確認**

Run: `git log --oneline -10`
Expected: 8〜9 個の `#2 ...` コミットが並ぶ (spec のコミット含む)。

`npm run dev` のブラウザ動作確認は PR 作成後にユーザに依頼する。
